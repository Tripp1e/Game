package org.jantor.level.element.collectable;

import org.jantor.image.CollectableImage;
import org.jantor.level.element.Element;

public abstract class Collectable extends Element {
    public Collectable(CollectableImage image) {
        super(image);
    }
}
